name: Deploy and Test

on:
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # Your deployment steps here
      # ...

      - name: Trigger Bugster Tests
        uses: Bugsterapp/bugster-action@v1
        with:
          bugster_api_key: ${{ secrets.BUGSTER_API_KEY }}
          organization_id: "org_2z6VhCOkBTNJC5eFNLw3RZ0zv1R"
          project_id: "francosbenitez.com-1763494219"
          environment_url: "https://www.francosbenitez.com/"
