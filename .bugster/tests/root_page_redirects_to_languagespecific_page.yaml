name: Root page redirects to language-specific page
page: /
page_path: app/page.tsx
task: Test if root path redirects to a language-specific page
steps:
- Navigate to the root URL path (/)
- Observe if redirection occurs
- Verify the URL changes to include a language code (e.g., /en)
- Verify the page content loads in the expected language
expected_result: User should be redirected from / to a language-specific path (e.g.,
  /en)
