name: Verify theme toggle functionality switches between light and dark modes
page: home
page_path: app/[lang]/page.tsx
task: Test theme switching between light and dark modes
steps:
- Navigate to the home page
- Click the theme toggle button in the header
- Verify the page background and text colors change to reflect the new theme
- Click the theme toggle again to verify it switches back
expected_result: Page appearance changes between light and dark themes when theme
  toggle is clicked
